# Web framework & transport
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
websockets>=12.0

# Audio / VAD
numpy>=1.24.0
webrtcvad>=2.0.10
# Optional recording: PCM → MP3 (requires ffmpeg on system)
pydub>=0.25.0

# HTTP client for remote ASR
httpx>=0.26.0

# TTS (untuk balasan asisten; local). 7.2.7+ perbaiki 403 dari Bing Speech.
edge-tts>=7.2.7

# Config
pydantic-settings>=2.1.0

# PostgreSQL (when TRANSCRIPT_STORAGE=postgresql or USE_DATABASE=true)
sqlalchemy[asyncio]>=2.0.25
asyncpg>=0.29.0
greenlet>=3.0.0

# Cloud object storage for session recordings (pick one backend via OBJECT_STORAGE_BACKEND)
supabase>=2.4.0
firebase-admin>=6.5.0
